import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.ernsci.ngl",
  appName: "ERENNGL",
  webDir: "public",
  androidScheme: "https",
};

export default config;